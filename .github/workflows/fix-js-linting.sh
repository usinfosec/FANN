#!/bin/bash

# This script is no longer needed as we're using a simpler approach in the CI workflow
# We now run ESLint with '|| true' to show warnings but not fail the build

echo "This script is deprecated and no longer used."
exit 0
